#include stdio.h

int main() 
{
  int num[5];
  int min,max,i,j;
  
  for(i=0; i<5; i++)
 {
    printf(Enter Number #%d ,i+1);
    scanf(%d,&num[i]); 
 }
min=num[0];
for(j=0; j<5; j++)
{
   if(minnum[j])
   min=num[j];
   else if (minnum[j])
   max=num[j];
   else{}
}
 printf(Max Number is %d,max);
return 0;
} 